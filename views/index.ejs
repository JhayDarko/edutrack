<% title = 'Inicio' %>

<div class="d-flex flex-column justify-content-center align-items-center min-vh-100 bg-light text-center px-3">
  <img src="/img/logo.png" alt="Logo <%= appName %>" class="mb-4 animate-fade-in" style="width:120px; max-width:100%;">
  <h1 class="display-3 fw-bold mb-3 text-primary animate-slide-up">Bienvenido a <%= appName %></h1>
  <p class="lead mb-4 text-secondary fw-semibold animate-slide-up-delay">Tu plataforma educativa profesional para crecer y aprender</p>
  <a href="/login" class="btn btn-lg btn-primary px-5 shadow-lg animate-pulse-hover" style="border-radius: 50px; font-weight: 600;">
    Iniciar Sesión <i class="fa-solid fa-arrow-right ms-2"></i>
  </a>
</div>

<style>
  /* Animaciones suaves */
  .animate-fade-in {
    animation: fadeIn 1.2s ease forwards;
  }
  .animate-slide-up {
    opacity: 0;
    transform: translateY(30px);
    animation: slideUp 1s ease forwards;
  }
  .animate-slide-up-delay {
    opacity: 0;
    transform: translateY(30px);
    animation: slideUp 1s ease forwards;
    animation-delay: 0.3s;
  }
  .animate-pulse-hover:hover {
    animation: pulse 1.2s infinite;
  }

  @keyframes fadeIn {
    to { opacity: 1; }
  }
  @keyframes slideUp {
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }
</style>
